import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function (_pi: ExtensionAPI) {
	// pi-limit scaffold: /orgm-limits remains in pi-harness until extraction lands.
}
